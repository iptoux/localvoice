# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# whisper.cpp binaries and DLLs — too large for git; bundled into the installer
# at build time. For development, copy the whisper.cpp Windows release zip contents
# into src-tauri/binaries/ (whisper-cli.exe + ggml.dll + whisper.dll etc. flat).
src-tauri/binaries/whisper-bin-*/
src-tauri/binaries/*.exe
src-tauri/binaries/*.dll

# Environment variables
.env
.env.local
.env.*.local

# Test coverage
coverage
*.lcov

# Storybook build artifacts
storybook-static

# Test results
test-results/
playwright-report/

# Build artifacts
*.exe
*.msi
*.dmg
*.app
*.deb
*.rpm
*.AppImage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
plan/
